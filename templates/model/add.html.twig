{% extends 'base.html.twig' %}

{% block body %}
    {% if(message is empty) %}
    {{ form(form) }}
    {% else %}
        {{ message }}
    {% endif %}


{% endblock %}

{% block title %}

{% endblock %}

