{% extends 'base.html.twig' %}

{% block title %}CategoryTag{% endblock %}

{% block body %}
    <h1>CategoryTag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_tag_index') }}">back to list</a>

    <a href="{{ path('category_tag_edit', {'id': category_tag.id}) }}">edit</a>

    {{ include('category_tag/_delete_form.html.twig') }}
{% endblock %}
