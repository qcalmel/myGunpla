{% extends 'base.html.twig' %}

{% block title %}Edit Scale{% endblock %}

{% block body %}
    <h1>Edit Scale</h1>

    {{ include('scale/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('scale_index') }}">back to list</a>

    {{ include('scale/_delete_form.html.twig') }}
{% endblock %}
