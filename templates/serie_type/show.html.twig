{% extends 'base.html.twig' %}

{% block title %}SerieType{% endblock %}

{% block body %}
    <h1>SerieType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ serie_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ serie_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('serie_type_index') }}">back to list</a>

    <a href="{{ path('serie_type_edit', {'id': serie_type.id}) }}">edit</a>

    {{ include('serie_type/_delete_form.html.twig') }}
{% endblock %}
